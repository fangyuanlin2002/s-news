version: 1
frontend:
    phases:
        preBuild:
            commands:
                - yarn
        build:
            commands:
                - yarn build:poc
    artifacts:
        baseDirectory: dist
        files:
            - "**/*"
    cache:
        paths:
            - node_modules/**/*
env:
    variables:
        NEXT_PUBLIC_BACKEND_URL: "https://46upjedrtf.execute-api.ap-southeast-1.amazonaws.com/poc"
