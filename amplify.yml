version: 1
applications:
    - appRoot: ben_playground
      frontend:
          phases:
              preBuild:
                  commands:
                      - yarn
              build:
                  commands:
                      - yarn build:poc
          artifacts:
              baseDirectory: .next
              files:
                  - "**/*"
          cache:
              paths:
                  - node_modules/**/*
      env:
          variables:
              NEXT_PUBLIC_BACKEND_URL: "http://18.166.74.144:8000"
